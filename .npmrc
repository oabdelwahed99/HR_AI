# Suppress funding messages (not critical)
fund=false

# Note: Deprecation warnings for transitive dependencies (rimraf, inflight, glob, etc.)
# are from dependencies of dependencies. They will be automatically updated when
# the parent packages (like Next.js) update their dependencies.
# These warnings don't affect functionality and can be safely ignored.
